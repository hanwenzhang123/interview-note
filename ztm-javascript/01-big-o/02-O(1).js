O(1) - Constant time
does not matter how big our inputs are, always going to do the constant amount of time on a function
same number of operation, a flat line, 1 here, can change to different number
input can be any time of data, not just arrays

function compressFirstBox(boxes){
  console.log(boxes[0]);  //always grabbing the first item in the array
}


const boxes = [0, 1, 2, 3, 4, 5];

function logFirstTwoBoxes(boxes) {
  console.log(boxes[0]);
  console.log(boxes[1]);
}

logFirstTwoBoxes(boxes)
